# Bug 修复与安全加固计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 修复 4 个已确认的 Bug（含 2 个严重 Bug）和 2 个安全漏洞

**Architecture:** 改动集中在 `js/ui.js`（竞态条件、难度可见性、回放泄漏）和 `js/game.js`（文件上传校验），均为单函数内局部修改，无跨模块影响

**Tech Stack:** 纯 JavaScript，无外部依赖

---

### Task 1: 修复 AI 竞态条件

**Bug:** `triggerAIMove()` 的 `setTimeout` 在游戏重启后仍会执行，导致 AI 在空白棋盘上落子

**Files:**
- Modify: `js/ui.js:171-185`（`triggerAIMove` 函数）
- Modify: `js/ui.js:303-319`（`restartGame` 函数）

- [ ] **Step 1: 为 triggerAIMove 添加可取消的 timer**

```javascript
function triggerAIMove() {
  GameState.isAIThinking = true;
  updateStatus();

  // 清除之前的 AI 计时器，防止竞态
  if (GameState._aiTimer) {
    clearTimeout(GameState._aiTimer);
  }

  GameState._aiTimer = setTimeout(() => {
    const aiMove = getAIMove(GameState.board.grid, GameState.difficulty);

    if (aiMove) {
      placeStone(aiMove.row, aiMove.col);
    }

    GameState.isAIThinking = false;
    GameState._aiTimer = null;
  }, 500);
}
```

- [ ] **Step 2: restartGame 中清除 AI 计时器**

在 `restartGame()` 函数中，`GameState.isAIThinking = false;` 之后添加：

```javascript
// 清除 AI 计时器，防止竞态
if (GameState._aiTimer) {
  clearTimeout(GameState._aiTimer);
  GameState._aiTimer = null;
}
```

- [ ] **Step 3: 验证**

在浏览器中打开游戏，快速执行以下操作确认修复：
1. 选择人机模式
2. 点击落子触发 AI 思考
3. 在 500ms 内点击"重新开始"
4. 确认棋盘重置后无 AI 棋子自动出现

- [ ] **Step 4: Commit**

```bash
git add js/ui.js
git commit -m "fix: 修复 AI 竞态条件 — 重启后残影落子

triggerAIMove 的 setTimeout 在 restartGame 后仍执行。
现在存储计时器 ID 并在重启时清除。

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 2: 修复难度选择器永远不可见

**Bug:** 切换到人机模式时，难度 `<select>` 的 `.hidden` class 未被移除

**Files:**
- Modify: `js/ui.js:69-75`（模式切换事件处理器）

- [ ] **Step 1: 在模式切换事件中添加难度选择器可见性切换**

在 `bindEvents()` 的模式切换事件处理器中，`GameState.mode = e.target.dataset.mode;` 之后添加：

```javascript
// 切换难度选择器可见性
const difficultySelect = document.getElementById('difficulty');
if (difficultySelect) {
  difficultySelect.classList.toggle('hidden', GameState.mode !== 'ai');
}
```

完整的事件处理器：

```javascript
document.querySelectorAll('.mode-btn').forEach(btn => {
  btn.addEventListener('click', (e) => {
    document.querySelectorAll('.mode-btn').forEach(b => b.classList.remove('active'));
    e.target.classList.add('active');
    GameState.mode = e.target.dataset.mode;

    // 切换难度选择器可见性
    const difficultySelect = document.getElementById('difficulty');
    if (difficultySelect) {
      difficultySelect.classList.toggle('hidden', GameState.mode !== 'ai');
    }

    restartGame();
  });
});
```

- [ ] **Step 2: 验证**

1. 打开游戏，切换到人机模式 → 确认难度选择器出现
2. 切回双人模式 → 确认难度选择器消失
3. 在人机模式下选择不同难度开始游戏，确认 AI 行为不同

- [ ] **Step 3: Commit**

```bash
git add js/ui.js
git commit -m "fix: 难度选择器始终隐藏 — 模式切换时 toggle hidden class

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 3: 修复回放进行中重启不停止定时器

**Bug:** `restartGame()` 未调用 `replayManager.stopReplay()`，回放定时器在后台持续运行

**Files:**
- Modify: `js/ui.js:303-319`（`restartGame` 函数）

- [ ] **Step 1: restartGame 中停止回放**

在 `restartGame()` 函数开头添加回放停止逻辑：

```javascript
function restartGame() {
  // 停止回放（如果正在进行）
  if (GameState.isReplaying && window.replayManager) {
    window.replayManager.stopReplay();
    GameState.isReplaying = false;
  }

  // 清除 AI 计时器（Task 1 已添加）

  GameState.board.reset();
  GameState.isGameOver = false;
  GameState.isAIThinking = false;
  GameState.moveHistory = [];

  // ... 其余代码不变
}
```

- [ ] **Step 2: 验证**

1. 设置面板 → 录像 Tab → 点击一条录像开始回放
2. 回放进行中点击"重新开始"
3. 确认回放停止，棋盘重置正常

- [ ] **Step 3: Commit**

```bash
git add js/ui.js
git commit -m "fix: 重启时停止录像回放，防止定时器泄漏

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

### Task 4: 文件上传添加 MIME 类型校验

**安全问题:** 图片和音乐上传仅检查文件大小，依赖 HTML `accept` 属性（可被绕过）

**Files:**
- Modify: `js/game.js:173-184`（`fileToDataURL` 函数，添加 MIME 类型参数）
- Modify: `js/ui.js:367-381`（图片上传事件处理器）
- Modify: `js/ui.js:468-479`（音乐上传事件处理器）

- [ ] **Step 1: 增强 fileToDataURL 添加 MIME 类型校验**

```javascript
// 允许的 MIME 类型
const ALLOWED_IMAGE_TYPES = ['image/jpeg', 'image/png', 'image/webp'];

function fileToDataURL(file, allowedTypes = ALLOWED_IMAGE_TYPES) {
  return new Promise((resolve, reject) => {
    if (file.size > MAX_FILE_SIZE) {
      reject(new Error('图片大小不能超过 2MB'));
      return;
    }
    if (allowedTypes && !allowedTypes.includes(file.type)) {
      reject(new Error(`不支持的文件类型: ${file.type || '未知'}`));
      return;
    }
    const reader = new FileReader();
    reader.onload = () => resolve(reader.result);
    reader.onerror = () => reject(new Error('文件读取失败'));
    reader.readAsDataURL(file);
  });
}
```

- [ ] **Step 2: 音频上传添加 MIME 类型校验**

在 `AudioManager.loadCustomMusic()` 方法中添加：

```javascript
async loadCustomMusic(file) {
  const ALLOWED_AUDIO_TYPES = ['audio/mpeg', 'audio/wav', 'audio/ogg', 'audio/mp3', 'audio/x-wav', 'audio/x-m4a'];
  return new Promise((resolve, reject) => {
    if (file.size > this.MAX_FILE_SIZE) {
      reject(new Error('文件大小不能超过 10MB'));
      return;
    }
    if (file.type && !ALLOWED_AUDIO_TYPES.includes(file.type)) {
      reject(new Error(`不支持的音频格式: ${file.type}`));
      return;
    }
    const reader = new FileReader();
    reader.onload = (e) => {
      this.customMusicData = e.target.result;
      resolve(e.target.result);
    };
    reader.onerror = () => reject(new Error('文件读取失败'));
    reader.readAsDataURL(file);
  });
}
```

- [ ] **Step 3: 验证**

1. 尝试上传非图片文件（如 .js）作为背景 → 确认被拒绝
2. 尝试上传非音频文件（如 .exe）作为音乐 → 确认被拒绝
3. 上传合法的 JPG 图片和 MP3 音频 → 确认正常

- [ ] **Step 4: Commit**

```bash
git add js/game.js js/ui.js
git commit -m "fix: 文件上传添加 MIME 类型校验，防止绕过 accept 过滤

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>"
```

---

## 验证检查清单

- [ ] AI 人机模式：落子后立即重启，棋盘无残影棋子
- [ ] 难度选择器：人机模式可见，双人模式隐藏
- [ ] 录像回放中重启：回放停止，棋盘正常
- [ ] 非法文件上传被拒绝
- [ ] 合法文件上传正常
- [ ] 控制台无新增错误
